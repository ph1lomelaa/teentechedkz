"""Ставки вознаграждений по умолчанию — значения из регламента.

Единственный источник литералов для всех четырёх видов ставок. До появления
конструктора они лежали прямо в enum-свойствах моделей и дублировались строками
в TypeScript; теперь действующие ставки хранятся в таблице `reward_rules`, а
эти константы остаются подстраховкой: если строки правила нет или payload
битый, расчёт деградирует до сегодняшнего поведения, а не падает.

Модуль намеренно листовой — не импортирует ни модели, ни сервисы, чтобы его
могли использовать и модели (enum-свойства), и резолверы, и миграция.
"""
from __future__ import annotations

# Регламент менторов п.7.1 — доли этапов сопровождения, в процентах.
DEFAULT_STAGE_PCT: dict[str, int] = {
    "pre_admission": 30,
    "admission": 40,
    "post_admission": 30,
}

# Регламент менторов п.6.2 — санкции по цветовым статусам задач, ₸.
DEFAULT_TASK_PENALTY: dict[str, int] = {
    "yellow": 2_500,
    "orange": 5_000,
    "red": 7_500,
}

# Регламент МЗК п.6.6.1-6.6.3 — суммы по возвратным кейсам, ₸.
DEFAULT_REFUND_BONUS: dict[str, int] = {
    "yellow": 10_000,
    "orange": 15_000,
    "red": 25_000,
}

# Регламент МЗК п.7.5 — пороги месячного бонуса ОКК. Порядок по убыванию:
# резолвер берёт первый порог, который набранный процент перекрывает.
DEFAULT_MZK_BONUS_TIERS: list[dict[str, float | int]] = [
    {"min_score_pct": 90, "amount": 20_000},
    {"min_score_pct": 80, "amount": 10_000},
]
