# ТЗ: UI-редизайн TeenTechEd CRM (ветка `ui/design-refresh`)

**Автор:** Aziza · **Дата:** 2026-07-24
**Рамки:** только UI. Никакие данные, API-вызовы, обработчики, react-query-ключи, роуты и структуры данных не меняются. Каждое отображаемое сейчас поле данных остаётся видимым после редизайна.

---

## 1. Контекст и главный вывод анализа

Сравнивались две кодовые базы:

- **Vue-проект** (`~/Documents/TeenTeached`, Vue 3 + Tailwind) — исходная дизайн-система: тёмная тема `#0A0A0A`, жёлтый акцент `#FFD400`, Archivo для заголовков/цифр, Manrope для текста, карточки `#141414` с рамками `#2A2A2A`, фирменные приёмы (ghost-цифра 88px в стат-карточках, жёлтая засечка 4×20px у заголовков секций, hover-сдвиг рамки в `#C9A800`, градиентные hero-карточки).
- **React-репозиторий** (текущий) — **уже содержит портированную копию этой же дизайн-системы** (те же hex-токены, те же компоненты AppButton/StatCard/SegmentedTabs), но применённую только к workspace- и portal-оболочкам. Классические CRM-страницы (Статистика, База знаний, Общая база, Карточка студента, Шаблоны) используют её частично и непоследовательно.

**Поэтому задача — не «переписать дизайн», а: (а) поднять типографику всего приложения, (б) довнедрить Vue-паттерны там, где их не хватает, (в) починить конкретные развалившиеся места (Статистика, База знаний, Roadmap в CRM).**

## 2. Диагностика (числа из анализа)

| Проблема | Факт |
|---|---|
| Мелкий текст | 763 использования шрифтов ≤12px; из них 340 — произвольные `text-[9px]…text-[11.5px]`. В `tailwind.config.js` вообще нет секции `fontSize`. |
| Плоская иерархия | Заголовки секций Статистики — 18px при тексте таблиц 14px и KPI-цифрах 36px; между 18 и 36px нет ни одного размера. |
| Статистика | Одна колонка на ~7 экранов, без вкладок/якорей; все статус-плашки 10px капсом;風 имена в риск-листе — не ссылки; риск-лист обрезан на 15 без ссылки на `/at-risk`. |
| База знаний | Вкладок нет вообще: ощущение «вкладка во вкладке» — это 5 слоёв одинаковых рамочных коробок (категория → модалка → `<details>` до 6 уровней). Нет поиска, узкая модалка `max-w-3xl`/70dvh, статья: заголовок H2 всего 1.15rem против текста 0.875rem. |
| Roadmap | Три параллельные реализации таймлайна; в CRM-виде дата — сырой ISO, плашки 9–9.5px, `description`/`expected_result` вообще не показываются нигде в CRM/workspace-редакторах. |
| Баг токенов | `--p-accent`/`--p-accent-dim` не проброшены в `.crm-shell` → warn-подсветка StatCard и eyebrow PageHeader на CRM-страницах молча не работают. |
| Ghost-цифра | `text-white opacity-[0.03]` — невидима в светлой теме. |
| Оболочка CRM | Нет max-width (контент растягивается на всю ширину), группы навигации в сайдбаре определены, но не рендерятся. |

## 3. Что берём из Vue (эталонные паттерны)

1. **Лестница типографики**: h1 30–36px black → заголовок секции 16–20px extrabold **с жёлтой засечкой 4×20px + чип-счётчиком** → названия строк 13.5–15px bold → мета 12–13px → микро-лейблы uppercase+tracking только для декоративных ярлыков.
2. **Секционные заголовки**: `<span class="h-5 w-1 rounded bg-accent"/>` + Archivo extrabold + чип-счётчик — мгновенная сканируемость.
3. **Карточка-строка задачи**: радиус 13px, `px-[18px] py-[15px]`, hover `translate-x-[3px]` + рамка `#C9A800`, чекбокс 22px, счётчик подзадач, PriorityPill/StatusPill с полупрозрачными тинтами (`bg-accent/15 text-accent`, `bg-good/15 text-good`).
4. **Hero-карточка roadmap**: градиент `#161616→#111111`, ghost-флаг страны 220px при 16% прозрачности, ряд чипов (год — жёлтый филл, остальные — panel2), 4 метрики (число 20px жёлтым Archivo + подпись 11px), градиентный прогресс-бар `#C9A800→#FFD400`. *(В React уже есть как `RoadmapHeaderCard` — переиспользовать, не дублировать.)*
5. **Таймлайн этапов**: узлы 46px с номерами, halo `rgba(255,212,0,.14)` 5px у активного, заполнение линии с полу-кредитом за `in_progress`.
6. **Statuspill-язык**: жёлтый = действие/прогресс, зелёный — только «Готово», серые `#232323`/`#1a1a1a` — нейтральное. На жёлтом всегда чёрный текст.
7. **Скелетоны, повторяющие геометрию** финальных блоков; `.fade-in` (0.35s, 8px подъём) на смену видов.
8. **Дисциплина ширины**: контент `max-w-[1180px]`, отступы `px-5 md:px-8 py-7`.

## 4. Изменения — по блокам

### Фаза 1. Глобальные токены и типографика (одним коммитом)

**4.1 `tailwind.config.js` — добавить `fontSize`:**
```js
fontSize: {
  '2xs': ['0.75rem',   { lineHeight: '1rem' }],     // 12px — чипы, таблично-заголовочный капс
  xs:    ['0.8125rem', { lineHeight: '1.125rem' }], // 13px (было 12) — мета, подписи
  sm:    ['0.90625rem',{ lineHeight: '1.375rem' }], // 14.5px (было 14) — основной текст
  base:  ['1rem',      { lineHeight: '1.5rem' }],
  // остальные — Tailwind-дефолты, прописать явно
}
```
Одно это изменение поднимает 388 `text-xs` и 538 `text-sm` по всему приложению.

**4.2 `src/index.css`:**
- Пробросить `--p-accent: #FFD400` и `--p-accent-dim: #C9A800` в блок `.crm-shell` (и light-вариант `#FFD400`/`#8A6F00`) — фикс warn-подсветки.
- `.label-caps`: 11px → 12px (`text-2xs`-эквивалент).
- `.kb-content` (типографика статей Базы знаний): h2 1.15rem→1.5rem, h3 1.05rem→1.25rem, h4 0.95rem→1.05rem; p/ul/ol 0.875rem→0.9375rem; таблицы/pre 0.8rem→0.875rem; `.kb-field-label` 0.7rem→0.75rem; `.kb-field-value` 0.85rem→0.9rem; **дифференцировать уровни вложенности**: `details` — рамка+фон как сейчас, `details details` — без рамки, только левая линия `border-left: 2px solid var(--border)` + отступ; `.kb-row summary` — 0.9375rem, semibold.
- `.auth-field-label` 10px → 11px.

**4.3 Кит-компоненты (`components/ui`, `components/ui/primitives`):**
| Файл | Было → Стало |
|---|---|
| `primitives/table.tsx` TableHead | `text-[11px]` → `text-2xs`; TableCell `p-4` → `px-4 py-3` (строки компактнее, текста больше на экран) |
| `primitives/badge.tsx` | `text-[11px]` → `text-2xs` |
| `AppButton.tsx` | sm `text-xs`→ok (станет 13px), md `text-[13px]`→`text-sm`; `rounded-xl`→`rounded-ctl` (унификация радиуса) |
| `AppInput.tsx` | поле `text-[13.5px]`→`text-sm`; лейбл `text-[11px]`→`text-2xs` |
| `SegmentedTabs.tsx` | `text-[12.5px]`→`text-xs` |
| `Pill.tsx` | `text-[11px]`→`text-2xs` |
| `StatusPill.tsx` | sm `text-[11px]`→`text-2xs`, md `text-[12px]`→`text-xs` |
| `PriorityPill.tsx` | `text-[10.5px]/[11px]`→`text-2xs`; убрать битый `text-ds-text` |
| `StatCard.tsx` | лейбл `text-[11px]`→`text-2xs`; ghost-цифра `text-white`→`text-current` (видима в обеих темах) |
| `PageHeader.tsx` | eyebrow оставить 11px (декоративный фирменный элемент), description `text-sm` (подрастёт сам) |
| `ProgressBar.tsx` | `text-[11px]`→`text-2xs` |
| `EmptyState.tsx` | description `text-sm` (сам подрастёт) |

**4.4 `components/shared/Layout.tsx` (CRM-оболочка):**
- Обернуть контент: `<main>` → внутренний `mx-auto w-full max-w-[1180px]` (как в Vue и WorkspaceLayout).
- Рендерить заголовки групп навигации (`НАВИГАЦИЯ`, `БАЗА ДАННЫХ`…): `text-[10px] uppercase tracking-[0.2em] text-white/35 px-3 pt-4 pb-1` — данные уже есть в `NavGroup[]`, сейчас `flatMap` их выбрасывает.
- `text-yellow-500` (eyebrow StudentCardPage) → `text-brand`.

### Фаза 2A. Roadmap (файлы: `shared/RoadmapTimeline.tsx`, `workspace/WorkspaceRoadmapEditor.tsx`, `portal/PortalRoadmap.tsx`)

Не трогаем: обработчики (`toggleTask/toggleSubtask/cycleStage/addTask/removeTask/…`), query-ключи, `window.prompt`-флоу (фаза 3, отдельным PR).

- **CRM RoadmapTimeline**: карточка задачи по Vue-рецепту — `rounded-[13px] px-[18px] py-[15px] hover:translate-x-[3px] hover:border-accent-dim`; чекбокс 22px; дата через `formatDate()` (dd.mm.yyyy) вместо сырого ISO; **показать `description` (2 строки, `line-clamp-2 text-xs text-p-muted`) и `expected_result` («Результат: …»), если они есть в данных** — данные уже приходят с API, это чисто UI; плашки статуса/приоритета — общий стиль тинтов 12px (заменить локальные 9–9.5px `StatusPill`/`PriorityPill` на компоненты из `components/ui` с едиными label'ами «В плане/В работе/Готово», «Обязательно/Рекомендуется/По желанию»); счётчик прогресса этапа — заметнее (12px bold + мини-бар).
- **WorkspaceRoadmapEditor**: те же правки (это тёмный близнец) + показать `description`/`expected_result`.
- **PortalRoadmap**: подтянуть к Vue-оригиналу — узлы 46px уже есть; выровнять размеры (названия задач 13px→`text-sm`, мета 11px→`text-xs`), дедлайн показывать **всегда** (сейчас скрыт, если есть description — потеря данных), формат dd.mm.yyyy; добавить PriorityPill в строки (данные есть, сейчас скрыты).
- Удалить мёртвые `components/ui/RoadmapCard.tsx` и `components/ui/RoadmapTimeline.tsx`? — **нет, не в этом PR** (не UI-риск, но лишний шум в диффе). Отметить в фазе 3.

### Фаза 2B. Статистика (`pages/StatisticsPage.tsx`)

Данные, вычисления, фильтры, сортировки — без изменений. Только организация и типографика:

1. **Секционная навигация**: `SegmentedTabs` (уже есть в ките, на странице не используется) с якорями-фильтром отображаемого блока: «Обзор · Воронка и риски · Услуги · Команда». Все данные остаются на странице (вкладки просто скроллят/показывают секции); дефолт — «Обзор» показывает всё как сейчас? — Нет: вкладка = показ соответствующих секций, «Обзор» = KPI-грид. Ничего не удаляется, меняется только группировка видимости.
2. **Заголовки секций**: 18px → `font-display text-xl font-black` (20px) + жёлтая засечка 4×20px + чип-счётчик (Vue-паттерн 2b).
3. **KPI-грид**: `mb-3`→`mb-10` (ритм); warn-карточки заработают после фикса `--p-accent`; карточка-ссылка «Финансы» — визуально отделить (ghost-рамка + стрелка), она навигация, а не метрика.
4. **Риск-лист**: имена студентов — `<Link to={/students/:id}>`; под таблицей строка «Показаны 15 из N · Открыть полный риск-лист →» (ссылка на `/at-risk`). Плашки статусов 10px → `StatusPill`-стиль 12px.
5. **Бар-чарты**: единая ширина колонки лейблов (w-44), убрать фантомные 2–3% бары при нуле (нулевое значение — просто пустой трек + «0»), счётчики баров `text-sm`→`text-base font-bold tabular-nums`.
6. **Чипы фильтров**: убрать мёртвый `mb-8`-гэп, когда чипов нет.
7. Скелетоны вместо `'…'` в KPI при загрузке (геометрия = финальная карточка).

### Фаза 2C. База знаний (`pages/KnowledgeBasePage.tsx` + `.kb-content` из фазы 1)

Сохраняем: `openId`-контракт, query-ключи, sync-флоу, Radix Dialog.

1. **Вкладки категорий** (`SegmentedTabs`): «Все · Мифы и разборы · Стипендии · Регламенты · Пакеты и выплаты · Шаблоны» — клиентская фильтрация уже загруженного списка (данные не трогаем). Активная категория — в `useState`.
2. **Поиск по заголовкам** (клиентский `filter` по `title`) — инпут с иконкой по Vue-рецепту.
3. **Строки статей**: `py-3`→`py-3.5`, заголовок `text-sm font-medium`→`text-sm font-bold`; добавить чип категории в режиме «Все».
4. **Модалка статьи**: `max-w-3xl`→`max-w-4xl`, тело `max-h-[70dvh]`→`max-h-[80dvh]`, один скролл-контекст (убрать двойной); заголовок статьи `text-xl`→`font-display text-2xl font-black`.
5. Типографика статьи + дифференциация вложенности — уже в фазе 1 (`.kb-content`).

### Фаза 2D. Зачистка мелкого текста на тяжёлых страницах

`StudentCardPage.tsx` (54 вхождения), `FinancesPage.tsx` (51), `StudentsListPage.tsx` (45), `WorkspaceStudentDetailPage.tsx` (45), `TemplatesPage.tsx` (36): механическая замена `text-[9px]|[10px]|[10.5px]|[11px]|[11.5px]` → `text-2xs` (чипы/лейблы) или `text-xs` (мета/значения), `text-[12px]|[12.5px]|[13px]|[13.5px]` → `text-xs`/`text-sm`. Без изменения разметки.

## 5. Что сознательно НЕ делаем в этом PR (фаза 3, отдельно)

- Замена `window.prompt`/`window.confirm` на модалки (меняет UX-флоу — согласовать).
- Удаление мёртвого кода (CrmPageHeader, ui/RoadmapCard, ui/RoadmapTimeline, `.nav-item*`).
- Слияние трёх реализаций roadmap-таймлайна в одну.
- Вынос инлайн-копии FilterPopover из StudentsListPage.
- URL-state для открытой статьи Базы знаний.

## 6. Критерии приёмки

1. `npm run build` (tsc + vite) — зелёный.
2. Ни одно отображаемое поле данных не исчезло; в Roadmap данных стало **больше** (description/expected_result/дедлайны/приоритеты, которые уже приходили с API, но скрывались).
3. Ни один файл в `src/api/`, `src/hooks/`, `src/contexts/` не изменён; обработчики и query-ключи не тронуты (проверка диффа).
4. Диф не содержит изменений вне `frontend/` (кроме этого файла).
5. Обе темы (dark/light) проверены на Статистике, Базе знаний, Roadmap, Общей базе.

## 7. План коммитов (для PR)

1. `docs: ТЗ UI-редизайна` — этот файл.
2. `ui: глобальная шкала шрифтов, фикс --p-accent, кит-компоненты` — фаза 1.
3. `ui: roadmap — карточки, чипы, даты, description` — фаза 2A.
4. `ui: статистика — секции, иерархия, ссылки` — фаза 2B.
5. `ui: база знаний — вкладки, поиск, типографика статей` — фаза 2C.
6. `ui: зачистка мелкого текста на тяжёлых страницах` — фаза 2D.
